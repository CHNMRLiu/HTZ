services:
  backend:
    build: ./backend
    ports:
      - "5671:5671"
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "5670:5670"
    depends_on:
      - backend
    restart: unless-stopped
